<?php

$title = 'My post';
$this->headTitle($title);
?>

<h1>
    <?= $this->escapeHtml($title);
    ?>
</h1>
<p>
    <a href="<?= $this->url('post', ['action' => 'add']) ?>" class="btn btn-info">Add new post</a>
    <a href="<?= $this->url('category', ['action' => 'index']) ?>" class="btn btn-info">Manage categories</a>
</p>
<table class="table">
    <tr>
        <th>Title</th>
        <th>Content</th>
        <th>Category</th>
        <th>&nbsp;</th>
    </tr>
    <?php foreach ($posts as $post) : ?>
        <tr>
            <td><?= $this->escapeHtml($post->title); ?></td>
            <td><?= $this->escapeHtml($post->content); ?></td>
            <td><?= $this->escapeHtml($post->category->categoryName); ?></td>

            <td>
                <a href="<?= $this->url('comment', ['action' => 'add', 'id' => $post->id]) ?>">Add comment</a>
            </td>
            <td>
                <a href="<?= $this->url('post', ['action' => 'edit', 'id' => $post->id]) ?>">Edit</a>
            </td>
            <td>
                <a href="<?= $this->url('post', ['action' => 'delete', 'id' => $post->id]) ?>">Delete</a>
            </td>
        </tr>

    <?php endforeach; ?>
</table>
